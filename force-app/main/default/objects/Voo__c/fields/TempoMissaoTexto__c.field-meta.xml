<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>TempoMissaoTexto__c</fullName>
    <description>Extenso do tempo da missão</description>
    <externalId>false</externalId>
    <formula>IF(
				CEILING(MOD(TempoMissao__c ,60)) = 0 ||
				CEILING(MOD(TempoMissao__c ,60)) = 60,
				IF(
				    CEILING(MOD(TempoMissao__c ,60)) = 0,
			     TEXT(FLOOR(TempoMissao__c / 60))&amp;&apos;h&apos;,
				    TEXT(FLOOR(TempoMissao__c / 60)+1)&amp;&apos;h&apos;
				),
				TEXT(FLOOR(TempoMissao__c / 60))&amp;&apos;h &apos;&amp;
    TEXT(CEILING(MOD(TempoMissao__c ,60)))&amp;&apos;min&apos;
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Tempo Missão</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
