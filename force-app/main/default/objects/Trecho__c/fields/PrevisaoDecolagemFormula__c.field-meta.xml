<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PrevisaoDecolagemFormula__c</fullName>
    <description>Quando a decolagem do trecho for preenchido, irá gerar a &apos;Previsão de pouso (fórmula)&apos; para preencher no template de e-mail (takeoff report) o horário do pouso</description>
    <formula>Decolagem__c+ (TempoMissao__c/1440)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Quando a decolagem do trecho for preenchido, irá gerar a &apos;Previsão de pouso (fórmula)&apos; para preencher no template de e-mail (takeoff report) o horário do pouso</inlineHelpText>
    <label>Previsão de decolagem (fórmula)</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>DateTime</type>
</CustomField>
